# api

To install dependencies:

```bash
bun install || npm install
```

To run dev:

```bash
bun run dev || npm run dev
``` 

To run for production:
```bash
bun run start || npm run start
```

this project was created with bun, but you can use with nodejs  
